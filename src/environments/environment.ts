/*export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080' // A fonte da verdade única!
};*/

export const environment = {
  production: true,
  // IMPORTANTE: Sem a barra '/' no final
  apiBaseUrl: 'https://vendas-api-prod.onrender.com'
};
